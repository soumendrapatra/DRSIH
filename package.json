{
  "name": "retina-ai-viewer",
  "version": "1.0.0",
  "description": "Retina AI Analysis Viewer — Wizard-of-Oz AI Demo",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build:data": "node scripts/build-data.js",
    "build": "node scripts/build-data.js && npm run build --workspace=client",
    "server": "npm run dev --workspace=retina-ai-server",
    "client": "npm run dev --workspace=client",
    "dev": "concurrently --names server,client --prefix-colors cyan,yellow \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
